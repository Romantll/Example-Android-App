<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/logoImageView"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/ufp_logo"
            android:contentDescription="App Logo"
            android:layout_marginBottom="24dp"/>

        <Button
            android:id="@+id/btnEnterprise"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="USS Enterprise (NCC-1701)"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/btnVoyager"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="USS Voyager (NCC-74656)"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/btnDefiant"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="USS Defiant (NX-74205)" />
    </LinearLayout>
</ScrollView>
